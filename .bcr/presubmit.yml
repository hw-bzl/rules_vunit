bcr_test_module:
  module_path: "examples/simple"
  matrix:
    platform:
      - "ubuntu2004"
    bazel: [7.x, 8.x, 9.x]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
      test_targets:
        - "//..."
